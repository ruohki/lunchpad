import * as lodash from 'lodash';
import { v4 as uuid } from 'uuid';

/* 


export class VoiceMeeter extends Action {
  constructor(id: string = uuid()) {
    super(ActionType.SwitchPage, id);
  }
}

export class DoNothing extends Action {}


 */






/* 
type Method = 'POST' | 'GET' | 'PUT' | 'DELETE' | 'PATCH';
export class PerformWebrequest extends Action {
  public url: string
  public method: Method
  public headers: string
  public body: string

  constructor(url: string, method: Method, headers: string, body: string ) {
    super(ActionType.PerformWebrequest);

    this.url = url;
    this.method = method;
    this.headers = headers;
    this.body = body;
  }
}
 */



// OBS 


/* 

export class OBSToggleMixer extends Action {
  constructor(id: string = uuid()) {
    super(ActionType.OBSToggleMixer, id);
  }
}

export class OBSToggleFilter extends Action {
  constructor(id: string = uuid()) {
    super(ActionType.OBSToggleFilter, id);
  }
}

export class OBSToggleRecording extends Action {
  constructor(id: string = uuid()) {
    super(ActionType.OBSStartStopRecording, id);
  }
}

export class OBSToggleStream extends Action {
  constructor(id: string = uuid()) {
    super(ActionType.OBSStartStopStream, id);
  }
} */